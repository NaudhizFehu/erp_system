# ERP 시스템 미구현 페이지 개발 계획서

## 1. 개요

### 1.1 목적
현재 Sidebar에 정의되어 있으나 구현되지 않은 페이지들을 기존 회계 페이지 패턴을 기반으로 구현

### 1.2 기존 구현 패턴 분석
기존 회계/인사관리 페이지 분석 결과, 다음과 같은 공통 패턴 확인:

**프론트엔드 구조:**
- 검색 + 필터 (Search, Status, Date 등)
- 통계 카드 (Card 컴포넌트, 4개 그리드)
- 데이터 테이블/그리드 (Table 컴포넌트, 페이지네이션)
- 등록/수정 모달 (Dialog + Form)
- 삭제/확인 다이얼로그 (AlertDialog)
- 내보내기 기능 (CSV, Excel)

**상태 관리:**
- React Query 훅 (use{Entity}s, useCreate{Entity}, useUpdate{Entity}, useDelete{Entity})
- 검색어 디바운싱 (useDebounce)
- 페이지네이션 (useState + searchParams)
- 필터 상태 (useState)

**백엔드 구조:**
- Entity (JPA)
- Repository (Spring Data JPA)
- Service (비즈니스 로직)
- Controller (REST API)
- DTO (Request/Response)

---

## 2. 미구현 페이지 목록 및 우선순위

### 2.1 인사관리
1. **부서 관리 목록** (`/hr/departments`) - 우선순위: 높음
2. **직급 관리** (`/hr/positions`) - 우선순위: 높음

### 2.2 재고관리
3. **카테고리 관리** (`/inventory/categories`) - 우선순위: 중간
4. **재고 현황** (`/inventory/stock`) - 우선순위: 높음

### 2.3 영업관리
5. **주문 관리 목록** (`/sales/orders`) - 우선순위: 높음
6. **견적 관리** (`/sales/quotes`) - 우선순위: 중간

### 2.4 회계관리
7. ~~**전표 관리** (`/accounting/vouchers`)~~ - ✅ **완료** (TransactionList 라우팅 추가됨)
8. ~~**재무제표** (`/accounting/statements`)~~ - ✅ **완료** (FinancialStatementPage 라우팅 추가됨)

---

## 3. 페이지별 구현 계획

### 3.1 부서 관리 목록 (`/hr/departments`)

**현재 상태:**
- DepartmentDetail.tsx 존재 (상세 페이지)
- 목록 페이지 없음

**구현 내용:**

#### 백엔드
**이미 존재하는 것:**
- `Department` Entity
- `DepartmentRepository`
- API 엔드포인트 확인 필요

**추가 필요 사항:**
- `GET /api/hr/departments` - 목록 조회 (페이지네이션)
- `GET /api/hr/departments/search` - 검색
- `POST /api/hr/departments` - 부서 생성
- `PUT /api/hr/departments/{id}` - 부서 수정
- `DELETE /api/hr/departments/{id}` - 부서 삭제

#### 프론트엔드
**파일 구조:**
```
frontend/src/
├── pages/hr/
│   └── DepartmentList.tsx (신규)
├── components/hr/
│   └── DepartmentForm.tsx (신규)
├── hooks/
│   └── useDepartments.ts (확장)
└── types/
    └── hr.ts (기존)
```

**DepartmentList.tsx 구성:**
1. 헤더: 제목, 설명, 버튼 (추가, 새로고침, 내보내기)
2. 통계 카드: 총 부서 수, 활성 부서, 직원 수, 평균 직원 수
3. 검색 및 필터: 부서명 검색, 상태 필터
4. 부서 테이블:
   - 부서 코드
   - 부서명
   - 상위 부서
   - 직원 수
   - 상태 (활성/비활성)
   - 작업 (상세보기, 수정, 삭제)
5. 페이지네이션
6. 모달: 부서 등록/수정 폼
7. 삭제 확인 다이얼로그

**예상 작업 시간:** 4-6시간

---

### 3.2 직급 관리 (`/hr/positions`)

**현재 상태:**
- Position Entity 존재
- 관련 페이지 없음

**구현 내용:**

#### 백엔드
**추가 필요 사항:**
- `GET /api/hr/positions` - 목록 조회
- `GET /api/hr/positions/search` - 검색
- `POST /api/hr/positions` - 직급 생성
- `PUT /api/hr/positions/{id}` - 직급 수정
- `DELETE /api/hr/positions/{id}` - 직급 삭제
- `GET /api/hr/positions/stats` - 직급별 통계

#### 프론트엔드
**파일 구조:**
```
frontend/src/
├── pages/hr/
│   └── PositionList.tsx (신규)
├── components/hr/
│   └── PositionForm.tsx (신규)
└── hooks/
    └── usePositions.ts (확장)
```

**PositionList.tsx 구성:**
1. 헤더
2. 통계 카드: 총 직급 수, 활성 직급, 최고 직급, 최저 직급
3. 검색 및 필터: 직급명 검색, 레벨 필터
4. 직급 테이블:
   - 직급 코드
   - 직급명
   - 레벨
   - 직원 수
   - 상태
   - 작업
5. 모달 및 다이얼로그

**예상 작업 시간:** 3-5시간

---

### 3.3 카테고리 관리 (`/inventory/categories`)

**현재 상태:**
- Category Entity 존재 가능성 높음
- 관련 페이지 없음

**구현 내용:**

#### 백엔드
**추가 필요 사항:**
- `GET /api/inventory/categories` - 목록 조회
- `GET /api/inventory/categories/tree` - 계층 구조 조회
- `POST /api/inventory/categories` - 카테고리 생성
- `PUT /api/inventory/categories/{id}` - 카테고리 수정
- `DELETE /api/inventory/categories/{id}` - 카테고리 삭제

#### 프론트엔드
**파일 구조:**
```
frontend/src/
├── pages/inventory/
│   └── CategoryList.tsx (신규)
├── components/inventory/
│   └── CategoryForm.tsx (신규)
│   └── CategoryTree.tsx (신규) - 계층 구조 트리 뷰
└── hooks/
    └── useCategories.ts (신규)
```

**CategoryList.tsx 구성:**
1. 헤더
2. 통계 카드: 총 카테고리 수, 대분류, 중분류, 소분류
3. 검색 및 필터: 카테고리명 검색, 레벨 필터
4. 카테고리 테이블/트리:
   - 카테고리 코드
   - 카테고리명
   - 상위 카테고리
   - 레벨
   - 상품 수
   - 작업
5. 트리 뷰 모드 지원 (테이블/트리 전환)

**예상 작업 시간:** 5-7시간 (트리 구조 UI 포함)

---

### 3.4 재고 현황 (`/inventory/stock`)

**현재 상태:**
- Product Entity 존재
- ProductManagementPage 존재 (제품 관리)
- 재고 현황 전용 페이지 필요

**구현 내용:**

#### 백엔드
**추가 필요 사항:**
- `GET /api/inventory/stock` - 재고 현황 조회
- `GET /api/inventory/stock/low-stock` - 재고 부족 제품
- `GET /api/inventory/stock/movements` - 재고 이동 내역
- `GET /api/inventory/stock/stats` - 재고 통계

#### 프론트엔드
**파일 구조:**
```
frontend/src/
├── pages/inventory/
│   └── StockStatusPage.tsx (신규)
├── components/inventory/
│   └── StockMovementHistory.tsx (신규)
│   └── StockAdjustmentForm.tsx (신규)
└── hooks/
    └── useStock.ts (신규)
```

**StockStatusPage.tsx 구성:**
1. 헤더
2. 통계 카드: 총 재고 금액, 재고 부족, 과잉 재고, 이동 건수
3. 검색 및 필터: 제품명 검색, 카테고리 필터, 재고 상태 필터
4. 재고 현황 테이블:
   - 제품 코드
   - 제품명
   - 카테고리
   - 현재 재고
   - 안전 재고
   - 재고 상태 (정상/부족/과잉)
   - 최종 입고일
   - 작업 (조정, 이동 내역)
5. 재고 조정 모달
6. 이동 내역 모달

**예상 작업 시간:** 6-8시간

---

### 3.5 주문 관리 목록 (`/sales/orders`)

**현재 상태:**
- OrderDetail.tsx 존재 (상세 페이지)
- Order Entity 존재
- 목록 페이지 필요

**구현 내용:**

#### 백엔드
**이미 존재하는 것:**
- Order Entity
- OrderRepository

**추가 필요 사항:**
- `GET /api/sales/orders` - 목록 조회
- `GET /api/sales/orders/search` - 검색
- `POST /api/sales/orders` - 주문 생성
- `PUT /api/sales/orders/{id}` - 주문 수정
- `DELETE /api/sales/orders/{id}` - 주문 삭제
- `PUT /api/sales/orders/{id}/status` - 주문 상태 변경

#### 프론트엔드
**파일 구조:**
```
frontend/src/
├── pages/sales/
│   └── OrderList.tsx (신규)
├── components/sales/
│   └── OrderForm.tsx (신규)
│   └── OrderStatusBadge.tsx (신규)
└── hooks/
    └── useOrders.ts (확장)
```

**OrderList.tsx 구성:**
1. 헤더
2. 통계 카드: 총 주문 수, 진행 중, 완료, 취소
3. 검색 및 필터: 주문번호/고객명 검색, 상태 필터, 날짜 범위
4. 주문 테이블:
   - 주문번호
   - 주문일
   - 고객명
   - 총 금액
   - 상태 (대기/진행중/완료/취소)
   - 작업 (상세보기, 수정, 상태 변경)
5. 페이지네이션
6. 내보내기 기능

**예상 작업 시간:** 5-7시간

---

### 3.6 견적 관리 (`/sales/quotes`)

**현재 상태:**
- Quote Entity 존재 가능성
- 관련 페이지 없음

**구현 내용:**

#### 백엔드
**추가 필요 사항:**
- Quote Entity 생성 (존재하지 않는 경우)
- `GET /api/sales/quotes` - 목록 조회
- `POST /api/sales/quotes` - 견적 생성
- `PUT /api/sales/quotes/{id}` - 견적 수정
- `DELETE /api/sales/quotes/{id}` - 견적 삭제
- `PUT /api/sales/quotes/{id}/approve` - 견적 승인
- `POST /api/sales/quotes/{id}/convert-to-order` - 주문 전환

#### 프론트엔드
**파일 구조:**
```
frontend/src/
├── pages/sales/
│   └── QuoteList.tsx (신규)
│   └── QuoteDetail.tsx (신규)
├── components/sales/
│   └── QuoteForm.tsx (신규)
│   └── QuoteStatusBadge.tsx (신규)
└── hooks/
    └── useQuotes.ts (신규)
```

**QuoteList.tsx 구성:**
1. 헤더
2. 통계 카드: 총 견적 수, 대기 중, 승인, 거부, 주문 전환율
3. 검색 및 필터: 견적번호/고객명 검색, 상태 필터, 날짜 범위
4. 견적 테이블:
   - 견적번호
   - 견적일
   - 고객명
   - 총 금액
   - 유효기간
   - 상태 (작성/전송/승인/거부)
   - 작업 (상세보기, 수정, 승인, 주문 전환)
5. PDF 출력 기능

**예상 작업 시간:** 6-8시간 (주문 전환 로직 포함)

---

### 3.7 전표 관리 (`/accounting/vouchers`) - ✅ **완료**

**구현 완료 상태:**
- TransactionList.tsx 존재 (회계 전표)
- App.tsx에 라우팅 추가 완료 (line 134)

**구현 내용:**
```tsx
<Route path="/accounting/vouchers" element={<TransactionList />} />
```

**완료 일자:** 2025-12-30
**실제 작업 시간:** 1분 (라우팅만 추가)

---

### 3.8 재무제표 (`/accounting/statements`) - ✅ **완료**

**구현 완료 상태:**
- FinancialStatementPage.tsx 존재
- App.tsx에 라우팅 추가 완료 (line 141)
- **백엔드 인프라 완전 구현 완료:**
  - FinancialReport, FinancialReportItem 엔티티 존재
  - FinancialReportController (모든 SUPER_ADMIN 권한 추가 완료)
  - FinancialReportService, FinancialReportDto 구현됨
  - DdlForcer.java에 테이블 DDL 추가 완료 (lines 608-681)
    - financial_reports 테이블
    - financial_report_items 테이블
    - budgets 테이블 (lines 554-587)
    - budget_revisions 테이블 (lines 589-606)

**구현 내용:**
```tsx
<Route path="/accounting/statements" element={<FinancialStatementPage />} />
```

**완료 일자:** 2025-12-30
**실제 작업 시간:** 2시간
- 라우팅 추가: 1분
- DdlForcer.java 4개 테이블 DDL 추가: 30분
- FinancialReportController SUPER_ADMIN 권한 추가: 10분
- 기타 디버깅 및 검증: 1시간

**주요 수정 사항:**
1. accountingApi.ts - baseURL 중복 제거 (lines 29-30)
2. FinancialReportController.java - 모든 엔드포인트에 SUPER_ADMIN 역할 추가
3. DdlForcer.java - 회계 모듈 4개 테이블 CREATE 문 추가

**⚠️ 백엔드 재시작 필요:**
- DdlForcer.java 변경사항 적용을 위해 사용자가 백엔드를 재시작해야 합니다

---

## 3.9 구현 완료 요약 (2025-12-30)

### 완료된 작업
1. ✅ **회계 라우팅 추가** - App.tsx (lines 134-141)
   - `/accounting/vouchers` → TransactionList
   - `/accounting/statements` → FinancialStatementPage

2. ✅ **회계 데이터베이스 인프라 구축** - DdlForcer.java
   - budgets 테이블 (lines 554-587)
   - budget_revisions 테이블 (lines 589-606)
   - financial_reports 테이블 (lines 608-648)
   - financial_report_items 테이블 (lines 650-681)

3. ✅ **재무보고서 권한 수정** - FinancialReportController.java
   - 모든 @PreAuthorize 어노테이션에 SUPER_ADMIN 역할 추가

4. ✅ **API URL 중복 제거** - accountingApi.ts
   - `/api/api/...` 중복 방지 (lines 29-30)

5. ✅ **회사 목록 Page 객체 처리** - CompanyList.tsx
   - `response.data.data.content` 접근 수정 (lines 57-60)

### 남은 작업 (6개 페이지)
1. 부서 관리 목록 (`/hr/departments`) - 예상 8시간
2. 직급 관리 (`/hr/positions`) - 예상 7시간
3. 카테고리 관리 (`/inventory/categories`) - 예상 10시간
4. 재고 현황 (`/inventory/stock`) - 예상 13시간
5. 주문 관리 목록 (`/sales/orders`) - 예상 10시간
6. 견적 관리 (`/sales/quotes`) - 예상 13시간

**총 예상 잔여 시간:** 61시간 (약 8-10 영업일)

### 중요한 교훈
⚠️ **DdlForcer.java 필수 업데이트 규칙:**
- 프로젝트는 `ddl-auto: none` 설정으로 Hibernate 자동 테이블 생성 비활성화
- **모든 새 엔티티는 반드시 DdlForcer.java에 CREATE TABLE SQL 추가 필수**
- Entity만 만들고 DdlForcer.java 업데이트를 누락하면 `relation does not exist` 오류 발생
- 이 규칙은 `.clauderc` 파일에 명시되어 있음 (lines 50-55)

---

## 4. 공통 구현 패턴 템플릿

### 4.1 프론트엔드 페이지 구조 템플릿

```tsx
// {Entity}List.tsx
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Search, Plus, Filter, RefreshCw, Download } from 'lucide-react'

import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent } from '@/components/ui/alert-dialog'
import { {Entity}Form } from '@/components/{module}/{Entity}Form'
import { use{Entity}s, useCreate{Entity}, useUpdate{Entity}, useDelete{Entity} } from '@/hooks/use{Entity}s'
import { useDebounce } from '@/hooks/useDebounce'

export function {Entity}List() {
  const navigate = useNavigate()

  // 상태
  const [searchTerm, setSearchTerm] = useState('')
  const [page, setPage] = useState(0)
  const [size] = useState(20)
  const [showForm, setShowForm] = useState(false)
  const [formMode, setFormMode] = useState<'create' | 'edit'>('create')
  const [selected{Entity}, setSelected{Entity}] = useState<{Entity} | null>(null)

  const debouncedSearchTerm = useDebounce(searchTerm, 300)

  // API 훅
  const { data, isLoading, refetch } = use{Entity}s({ page, size, searchTerm: debouncedSearchTerm })
  const createMutation = useCreate{Entity}()
  const updateMutation = useUpdate{Entity}()
  const deleteMutation = useDelete{Entity}()

  // 통계 계산
  const stats = {
    total: data?.totalElements || 0,
    // ... 기타 통계
  }

  return (
    <div className="space-y-6">
      {/* 헤더 */}
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-3xl font-bold">{Entity} 관리</h1>
          <p className="text-muted-foreground">설명</p>
        </div>
        <div className="flex gap-2">
          <Button variant="outline" onClick={() => refetch()}>
            <RefreshCw className="mr-2 h-4 w-4" />
            새로고침
          </Button>
          <Button onClick={() => setShowForm(true)}>
            <Plus className="mr-2 h-4 w-4" />
            추가
          </Button>
        </div>
      </div>

      {/* 통계 카드 */}
      <div className="grid grid-cols-1 gap-4 md:grid-cols-4">
        {/* 통계 카드들 */}
      </div>

      {/* 검색 및 필터 */}
      <Card>
        <CardContent className="pt-6">
          <div className="relative">
            <Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" />
            <Input
              placeholder="검색..."
              value={searchTerm}
              onChange={(e) => setSearchTerm(e.target.value)}
              className="pl-10"
            />
          </div>
        </CardContent>
      </Card>

      {/* 데이터 테이블 */}
      <Card>
        <CardHeader>
          <CardTitle>{Entity} 목록</CardTitle>
        </CardHeader>
        <CardContent>
          <Table>
            {/* 테이블 내용 */}
          </Table>

          {/* 페이지네이션 */}
        </CardContent>
      </Card>

      {/* 등록/수정 모달 */}
      <Dialog open={showForm} onOpenChange={setShowForm}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>{formMode === 'create' ? '추가' : '수정'}</DialogTitle>
          </DialogHeader>
          <{Entity}Form
            {entity}={formMode === 'edit' ? selected{Entity} : undefined}
            onSubmit={handleSubmit}
            onCancel={() => setShowForm(false)}
          />
        </DialogContent>
      </Dialog>
    </div>
  )
}
```

### 4.2 백엔드 Controller 템플릿

```java
@RestController
@RequestMapping("/api/{module}/{entities}")
@RequiredArgsConstructor
@Slf4j
public class {Entity}Controller {

    private final {Entity}Service service;

    @GetMapping
    @PreAuthorize("hasAnyRole('ADMIN', 'MANAGER', 'USER')")
    public ResponseEntity<ApiResponse<Page<{Entity}Dto>>> get{Entity}s(
            @RequestParam(required = false) String searchTerm,
            @RequestParam(defaultValue = "0") int page,
            @RequestParam(defaultValue = "20") int size) {

        Pageable pageable = PageRequest.of(page, size);
        Page<{Entity}Dto> result = service.get{Entity}s(searchTerm, pageable);
        return ResponseEntity.ok(ApiResponse.success(result));
    }

    @GetMapping("/{id}")
    @PreAuthorize("hasAnyRole('ADMIN', 'MANAGER', 'USER')")
    public ResponseEntity<ApiResponse<{Entity}Dto>> get{Entity}(@PathVariable Long id) {
        {Entity}Dto result = service.get{Entity}(id);
        return ResponseEntity.ok(ApiResponse.success(result));
    }

    @PostMapping
    @PreAuthorize("hasAnyRole('ADMIN', 'MANAGER')")
    public ResponseEntity<ApiResponse<{Entity}Dto>> create{Entity}(
            @Valid @RequestBody {Entity}CreateRequest request) {
        {Entity}Dto result = service.create{Entity}(request);
        return ResponseEntity.ok(ApiResponse.success(result));
    }

    @PutMapping("/{id}")
    @PreAuthorize("hasAnyRole('ADMIN', 'MANAGER')")
    public ResponseEntity<ApiResponse<{Entity}Dto>> update{Entity}(
            @PathVariable Long id,
            @Valid @RequestBody {Entity}UpdateRequest request) {
        {Entity}Dto result = service.update{Entity}(id, request);
        return ResponseEntity.ok(ApiResponse.success(result));
    }

    @DeleteMapping("/{id}")
    @PreAuthorize("hasRole('ADMIN')")
    public ResponseEntity<ApiResponse<Void>> delete{Entity}(@PathVariable Long id) {
        service.delete{Entity}(id);
        return ResponseEntity.ok(ApiResponse.success(null));
    }
}
```

---

## 5. 구현 순서

### ~~Phase 1: 기반 페이지~~ - ✅ **회계 완료** (2025-12-30)
1. ~~부서 관리 목록 (`/hr/departments`)~~ → Phase 2로 이동
2. ~~직급 관리 (`/hr/positions`)~~ → Phase 2로 이동
3. ✅ ~~라우팅 확인 및 수정 (`/accounting/vouchers`, `/accounting/statements`)~~ **완료**

### Phase 2: 인사 관리 (1주)
1. 부서 관리 목록 (`/hr/departments`) - 8시간
2. 직급 관리 (`/hr/positions`) - 7시간

### Phase 3: 재고 관리 (1-2주)
3. 카테고리 관리 (`/inventory/categories`) - 10시간
4. 재고 현황 (`/inventory/stock`) - 13시간

### Phase 4: 영업 관리 (1-2주)
5. 주문 관리 목록 (`/sales/orders`) - 10시간
6. 견적 관리 (`/sales/quotes`) - 13시간

---

## 6. 기술 스택 및 라이브러리

### 프론트엔드
- **React 18** + TypeScript
- **React Query** (데이터 페칭)
- **React Router** (라우팅)
- **TailwindCSS** (스타일링)
- **shadcn/ui** (UI 컴포넌트)
- **lucide-react** (아이콘)
- **react-hot-toast** (알림)
- **date-fns** (날짜 처리)

### 백엔드
- **Spring Boot 3.2.0**
- **Java 17**
- **Spring Data JPA**
- **PostgreSQL**
- **Spring Security**
- **Lombok**
- **MapStruct** (DTO 매핑)

---

## 7. 검증 및 테스트

### 프론트엔드 테스트
- 검색 기능 (디바운싱 확인)
- 필터 기능 (다중 필터 조합)
- 페이지네이션 (이전/다음/페이지 번호)
- CRUD 작업 (생성/수정/삭제)
- 모달 열기/닫기
- 권한별 UI 표시 여부
- 로딩 상태 표시
- 에러 핸들링

### 백엔드 테스트
- 단위 테스트 (Service Layer)
- 통합 테스트 (Controller + Service)
- API 응답 검증
- 권한 검증
- 데이터 검증 (Validation)
- 에러 처리

---

## 8. 배포 전 체크리스트

### 코드 품질
- [ ] ESLint 검사 통과
- [ ] TypeScript 타입 에러 없음
- [ ] 불필요한 console.log 제거
- [ ] 주석 정리

### 기능 검증
- [ ] 모든 CRUD 작업 동작 확인
- [ ] 검색 및 필터 동작 확인
- [ ] 페이지네이션 동작 확인
- [ ] 권한별 접근 제어 확인
- [ ] 에러 메시지 표시 확인

### 성능
- [ ] 불필요한 API 호출 제거
- [ ] React Query 캐싱 확인
- [ ] 이미지 최적화
- [ ] 번들 크기 확인

### 문서화
- [ ] API 문서 작성 (Swagger/README)
- [ ] 컴포넌트 주석 작성
- [ ] README 업데이트

---

## 9. 예상 전체 작업 시간

| 페이지 | 백엔드 | 프론트엔드 | 테스트 | 총 시간 | 상태 |
|--------|--------|------------|--------|---------|------|
| 부서 관리 | 2시간 | 4시간 | 2시간 | 8시간 | 🔴 미완료 |
| 직급 관리 | 2시간 | 3시간 | 2시간 | 7시간 | 🔴 미완료 |
| 카테고리 관리 | 3시간 | 5시간 | 2시간 | 10시간 | 🔴 미완료 |
| 재고 현황 | 4시간 | 6시간 | 3시간 | 13시간 | 🔴 미완료 |
| 주문 관리 | 3시간 | 5시간 | 2시간 | 10시간 | 🔴 미완료 |
| 견적 관리 | 4시간 | 6시간 | 3시간 | 13시간 | 🔴 미완료 |
| ~~전표 관리~~ | - | - | - | ~~3시간~~ | ✅ **완료** |
| ~~재무제표~~ | 30분 | 1분 | - | ~~3시간~~ 2시간 | ✅ **완료** |
| **총계** | **18시간** | **29시간** | **14시간** | **61시간** | 6개 미완료 |

**예상 완료 기간:** 약 8-10 영업일 (1인 개발 기준)
**2025-12-30 현재 진행률:** 25% (8개 중 2개 완료)

---

## 10. 위험 요소 및 대응 방안

### 위험 요소
1. **Entity 미존재:** Quote, Category 등의 Entity가 없을 수 있음
2. **복잡한 비즈니스 로직:** 주문 상태 관리, 재고 조정 등 복잡한 로직
3. **권한 관리:** 역할별 접근 제어가 복잡할 수 있음
4. **데이터 일관성:** 트랜잭션 처리, 동시성 제어
5. **UI/UX 일관성:** 기존 페이지와 디자인 일관성 유지
6. ⚠️ **DdlForcer.java 누락 (발생함):** Entity 추가 시 DdlForcer.java 테이블 생성 SQL 누락

### 대응 방안
1. **Entity 확인 우선:** 백엔드 코드 검토 후 필요시 Entity 생성
2. **점진적 구현:** MVP 기능부터 구현 후 점진적 확장
3. **기존 패턴 준수:** 기존 회계/인사 페이지 패턴 철저히 따르기
4. **코드 리뷰:** 각 페이지 완료 후 리뷰 및 테스트
5. **문서화:** 복잡한 로직은 주석 및 문서화 필수
6. ✅ **DdlForcer.java 필수 체크:** 새 Entity 추가 시 반드시 DdlForcer.java에 CREATE TABLE SQL 추가 확인

### 실제 발생한 문제 및 해결 (2025-12-30)
**문제:** `relation "financial_reports" does not exist` 에러 발생
**원인:** Entity는 존재하지만 DdlForcer.java에 테이블 생성 SQL 누락
**해결:** DdlForcer.java에 4개 회계 테이블 DDL 추가 (budgets, budget_revisions, financial_reports, financial_report_items)
**교훈:** `.clauderc` 규칙 파일에 명시된 대로 ddl-auto: none이므로 모든 테이블은 DdlForcer.java에 수동 추가 필수

---

## 11. 참고 파일 목록

### 프론트엔드 참고 파일
```
frontend/src/pages/accounting/AccountList.tsx
frontend/src/pages/accounting/TransactionList.tsx
frontend/src/pages/hr/EmployeeManagement.tsx
frontend/src/components/accounting/AccountForm.tsx
frontend/src/components/accounting/JournalEntryForm.tsx
frontend/src/components/hr/EmployeeForm.tsx
frontend/src/hooks/useAccounts.ts
frontend/src/hooks/useAccounting.ts
frontend/src/hooks/useEmployees.ts
```

### 백엔드 참고 파일
```
backend/src/main/java/com/erp/hr/controller/EmployeeController.java
backend/src/main/java/com/erp/hr/entity/Employee.java
backend/src/main/java/com/erp/hr/entity/Department.java
backend/src/main/java/com/erp/hr/entity/Position.java
backend/src/main/java/com/erp/hr/dto/EmployeeDto.java
backend/src/main/java/com/erp/common/dto/PageResponse.java
```

---

## 12. 결론

본 개발 계획서는 기존 구현된 회계 및 인사 관리 페이지의 패턴을 분석하여 작성되었습니다.

**핵심 원칙:**
1. 기존 패턴 준수 (일관성)
2. 점진적 구현 (MVP 우선)
3. 충분한 테스트
4. 명확한 문서화
5. ⚠️ **DdlForcer.java 필수 업데이트** (새 엔티티 추가 시)

**2025-12-30 현재 진행 상황:**
- ✅ **완료:** 회계 라우팅 2개, 데이터베이스 테이블 4개, 권한 수정
- 🔴 **남은 작업:** 6개 페이지 (인사 2개, 재고 2개, 영업 2개)
- 📊 **진행률:** 25% (8개 중 2개 완료)
- ⏱️ **예상 잔여 시간:** 61시간 (약 8-10 영업일)

**다음 단계:**
1. ⚠️ **즉시 조치 필요:** 백엔드 재시작 (DdlForcer.java 테이블 생성 적용)
2. Phase 2: 인사 관리 페이지 구현 시작 (부서 관리, 직급 관리)
3. 백엔드 Entity 및 API 존재 여부 확인 후 순차적 구현
4. 각 페이지 완료 후 테스트 및 검증

---

## 13. 변경 이력

### 2025-12-30
- 회계 라우팅 완료 (`/accounting/vouchers`, `/accounting/statements`)
- DdlForcer.java 회계 테이블 4개 추가
- FinancialReportController SUPER_ADMIN 권한 추가
- accountingApi.ts baseURL 중복 제거
- CompanyList.tsx Page 객체 처리 수정
- 구현 순서 재조정 (Phase 재구성)
- 위험 요소에 DdlForcer.java 누락 사례 추가
- 현재 상황 요약 섹션 추가 (3.9)
