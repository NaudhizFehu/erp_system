# ERP 시스템 Nginx 설정
# erp.fehu.kr 도메인으로 접속

server {
    listen 80;
    listen [::]:80;
    server_name erp.fehu.kr;

    # HTTPS로 리다이렉트 (SSL 인증서 설정 후 활성화)
    # return 301 https://$server_name$request_uri;

    # 프론트엔드 정적 파일 서빙
    root /var/services/homes/naudhizfehu/erp-system/frontend;
    index index.html;

    # SPA를 위한 라우팅 설정 (모든 요청을 index.html로)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 요청은 백엔드로 프록시
    location /api/ {
        proxy_pass http://localhost:8991/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # 정적 파일 캐싱
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # gzip 압축
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_vary on;
}

# HTTPS 설정 (SSL 인증서 필요)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name erp.fehu.kr;
#
#     ssl_certificate /etc/ssl/certs/erp.fehu.kr.crt;
#     ssl_certificate_key /etc/ssl/private/erp.fehu.kr.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     root /var/services/homes/naudhizfehu/erp-system/frontend;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://localhost:8991/;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
#
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
#     gzip_vary on;
# }
