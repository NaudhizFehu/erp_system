# 배포 빠른 참조 가이드

**1분 안에 배포 시작하기**

---

## 🚀 지금 바로 배포하기

### 방법 1: 자동 스크립트 (권장)

```bash
# 프로젝트 루트에서
./scripts/deploy-dev.sh
```

**선택 옵션**:
- `1` - 프론트엔드만
- `2` - 백엔드만
- `3` - 전체 (프론트엔드 + 백엔드)

---

### 방법 2: 수동 배포

#### 프론트엔드

```bash
cd frontend
npm run lint && npm run type-check && npm run dev
```

#### 백엔드

IntelliJ IDEA에서 `Application.java` 실행

---

## 📝 체크리스트

### 배포 전
```
□ npm run lint (오류 0개)
□ npm run type-check (통과)
□ Git 커밋 메시지 준비
```

### 배포 후
```
□ http://localhost:5173 접속 확인
□ http://localhost:8080/swagger-ui.html 확인
□ 새 기능 테스트
□ 기존 기능 확인 (회귀 테스트)
```

---

## 🔧 주요 명령어

```bash
# 린트 검사
npm run lint

# 린트 자동 수정
npm run lint -- --fix

# 타입 체크
npm run type-check

# 빌드
npm run build

# 개발 서버
npm run dev
```

---

## 🆘 문제 해결

### 린트 오류
```bash
npm run lint -- --fix
npx prettier --write src/
```

### 포트 충돌
```bash
# 프론트엔드 (5173)
lsof -ti:5173 | xargs kill -9

# 백엔드 (8080)
lsof -ti:8080 | xargs kill -9
```

### 빌드 실패
```bash
rm -rf node_modules package-lock.json
npm install
```

---

## 📚 자세한 문서

- **배포 가이드**: `claudedocs/배포_가이드.md`
- **워크플로우**: `claudedocs/배포_워크플로우_다이어그램.md`
- **회계 모듈 계획**: `claudedocs/회계관리_모듈_구현계획.md`

---

## 🌐 접속 URL

| 서비스 | URL | 용도 |
|--------|-----|------|
| 프론트엔드 | http://localhost:5173 | 웹 애플리케이션 |
| 백엔드 API | http://localhost:8080 | REST API |
| Swagger UI | http://localhost:8080/swagger-ui.html | API 문서 & 테스트 |

---

## 📞 Git 작업

```bash
# 상태 확인
git status

# 변경사항 스테이징
git add .

# 커밋
git commit -m "feat: 기능 설명"

# develop 브랜치로 푸시
git checkout develop
git push origin develop
```

---

**빠른 시작**: `./scripts/deploy-dev.sh` → 옵션 선택 → 완료!
