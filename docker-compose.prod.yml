version: '3.8'

services:
  # ========================================
  # Blue 환경 (기본 활성)
  # ========================================

  # Blue 백엔드
  backend-blue:
    container_name: erp-backend-blue
    image: cursor-erp-backend:${VERSION:-latest}
    network_mode: bridge
    ports:
      - "8991:8991"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:postgresql://fehu.kr:5432/cursor_erp_system
      - SPRING_DATASOURCE_USERNAME=cursor_erp_system
      - SPRING_DATASOURCE_PASSWORD=cursor_erp_system
      - DEPLOYMENT_ENV=blue
      - SERVER_PORT=8991
    restart: unless-stopped
    labels:
      - "com.docker.compose.project=cursor-erp-system"
      - "project.name=cursor-erp-system"
      - "service.type=backend"
      - "deployment.env=blue"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8991/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Blue 프론트엔드
  frontend-blue:
    container_name: erp-frontend-blue
    image: cursor-erp-frontend:${VERSION:-latest}
    network_mode: bridge
    ports:
      - "8990:8990"
    environment:
      - BACKEND_HOST=192.168.0.30
      - BACKEND_PORT=8991
    depends_on:
      - backend-blue
    restart: unless-stopped
    labels:
      - "com.docker.compose.project=cursor-erp-system"
      - "project.name=cursor-erp-system"
      - "service.type=frontend"
      - "deployment.env=blue"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8990/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  # ========================================
  # Green 환경 (배포 시 활성화)
  # ========================================

  # Green 백엔드
  backend-green:
    container_name: erp-backend-green
    image: cursor-erp-backend:${VERSION:-latest}
    network_mode: bridge
    ports:
      - "8993:8991"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SPRING_DATASOURCE_URL=jdbc:postgresql://fehu.kr:5432/cursor_erp_system
      - SPRING_DATASOURCE_USERNAME=cursor_erp_system
      - SPRING_DATASOURCE_PASSWORD=cursor_erp_system
      - DEPLOYMENT_ENV=green
      - SERVER_PORT=8991
    restart: unless-stopped
    labels:
      - "com.docker.compose.project=cursor-erp-system"
      - "project.name=cursor-erp-system"
      - "service.type=backend"
      - "deployment.env=green"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8991/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    profiles:
      - green

  # Green 프론트엔드
  frontend-green:
    container_name: erp-frontend-green
    image: cursor-erp-frontend:${VERSION:-latest}
    network_mode: bridge
    ports:
      - "8992:8990"
    environment:
      - BACKEND_HOST=192.168.0.30
      - BACKEND_PORT=8993
    depends_on:
      - backend-green
    restart: unless-stopped
    labels:
      - "com.docker.compose.project=cursor-erp-system"
      - "project.name=cursor-erp-system"
      - "service.type=frontend"
      - "deployment.env=green"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8990/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    profiles:
      - green
